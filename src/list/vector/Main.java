package list.vector;

/**
 * Vector: O Vector é uma implementação antiga da interface List que é semelhante ao ArrayList,
 * mas é sincronizada, ou seja, é thread-safe. Isso significa que várias threads podem manipular
 * um objeto Vector ao mesmo tempo sem causar problemas de concorrência. No entanto, essa
 * sincronização adiciona uma sobrecarga de desempenho, tornando o Vector menos eficiente
 * do que o ArrayList em cenários em que a concorrência não é um problema. Por esse motivo,
 * o uso do Vector é menos comum em aplicações modernas.
 */

public class Main {
}
